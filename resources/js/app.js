import './bootstrap';

// RAMS UI behaviour layer — theme, sidebar, toasts, confirm dialogs,
// quick navigation, table filtering and form affordances.
import './ui';

// Import / export toolbar behaviour — print, row selection and live progress
// for queued imports. Every flow it touches works without it.
import './data-transfer';
